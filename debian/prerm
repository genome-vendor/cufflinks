#!/bin/sh
set -e

PKG="cufflinks"
VER="2.0.2"
PKG_DIR=/usr/lib/${PKG}${VER}

if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/bin/cufflinks ]; then
    for pathname in ${PKG_DIR}/bin/*; do
        filename=`basename $pathname`
        update-alternatives --remove ${filename}-fusion /usr/bin/${filename}${VER}
    done
fi
